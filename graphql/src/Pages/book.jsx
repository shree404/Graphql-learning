

const Book = ({ book }) => {
  return (
    <div>
      <h3>Book ID: {book.id}</h3>
      <p>Name: {book.name}</p>
      <h4>Authors: {book.email}</h4>
      <ul>
      </ul>
    </div>
  );
};

export default Book;
