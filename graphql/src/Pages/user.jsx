

const User = ({ user }) => {
  return (
    <div>
      <h3>User ID: {book.id}</h3>
      <p>Name: {book.name}</p>
      <h4>Email: {book.email}</h4>
      <ul>
      </ul>
    </div>
  );
};

export default User;
